options:
  tier: base
git:
  path: . # Relative to invocation. This assumes the project is running at the project root.
  repository: https://github.com/acederberg/captura-pipelines.git
  branch: master
  dockerfile: docker/dockerfile
  dockertarget: pipeline
  pull: False
image:
  target: pipelines
  repository: pipelines
  push: True

