options:
  tier: base
  push: False
git:
  repository: https://github.com/acederberg/captura-pipelines.git
  branch: master
  dockerdir: docker
  dockerfile: dockerfile
  dockertarget: test
image:
  target: pipeline
  repository: pipeline/test


